# Deploy Skill
1. Run the build to check for errors: `npm run build`
2. Fix any build errors found
3. Git add all changes, commit with a descriptive message, and push to main
4. Confirm the push was successful
